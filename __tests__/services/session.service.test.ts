import { describe, it } from "vitest"

describe("SessionService", () => {
  it.todo("deve rejeitar sessão sem voltas")
  it.todo("deve calcular bestLapMs corretamente")
  it.todo("deve rejeitar id duplicado para o mesmo usuário")
})
