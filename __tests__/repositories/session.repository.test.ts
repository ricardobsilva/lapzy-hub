import { describe, it } from "vitest"

describe("SessionRepository", () => {
  it.todo("deve criar sessão com id gerado pelo cliente")
  it.todo("deve retornar sessões ordenadas por date DESC")
  it.todo("deve filtrar sessões por trackId")
})
