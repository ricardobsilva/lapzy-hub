import { describe, it } from "vitest"

describe("TrackRepository", () => {
  it.todo("deve criar track com id gerado pelo cliente")
  it.todo("deve retornar tracks ordenados por updatedAt DESC")
  it.todo("deve retornar apenas tracks do userId informado")
})
